#include <iostream>

/*
Exercise 2.25: Determine the types and values of each of the following
variables.

(a) int* ip, i, &r = i;
    ip is a pointer, i is an int, r is a refrence to i
(b) int i, *ip = 0;
    i is an int, ip is a null pointer
(c) int* ip, ip2;
    ip is a pointer to int, ip2 is an int
*/

int main() {}